version: 2.1
jobs:
  print_world:
    docker:
      - image: cimg/node:17.2.0 # the primary container, where your job's commands are run
    steps:
      - checkout
      - run: echo "world"

  print_hello:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - checkout
      - run: echo "hello"


workflows:
  # Name the workflow "welcome"
  welcome:
    # Run the welcome/run job in its own container
    jobs:
      - print_world:
          depends:  [print_hello]
      - print_hello
  # build:
  #   docker:
  #     - image: cimg/node:17.2.0 # the primary container, where your job's commands are run
  #       auth:
  #         username: omarelsamahy109
  #         password: 0100*Name  # context / project UI env-var reference
  #   steps:
  #     - checkout # check out the code in the project directory
  #     - run: echo "hello world" # run the `echo` command
